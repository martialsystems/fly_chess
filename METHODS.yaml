question: does the wiring make the labeled move linearly easier than a shuffle?
falsifier: wiring_helped true on mix-1, or the real graph not silent after one LIF ply
object: remap / stitch
status: Closed
system: fly-shaped LIF graph; legal mask; shuffle control
data_version: fly-shaped graph as skin; not a MaleCNS game loop
neuron_table: reserved channels, not connectome IDs as chess pieces
dynamics: discrete LIF; mix-0 occupancy register plus search
rng: locks in logs/followup_lock.json, logs/search_lock.json, logs/mix1_distill.json, logs/value_lock.json
n_seeds: 1
n_meaning: mix-0 n=200 decisive; ethology n=40
locked_metric: wiring_helped is false; science_lock b4c1875
pre_specified: false
science_lock: b4c1875
parent_sha: ""
reduction: the fly is the skin; the player is the register plus search
